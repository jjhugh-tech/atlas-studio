use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "atlas-local-searxng-change-this"
  limiter: false

search:
  safe_search: 1
  formats:
    - html
    - json

outgoing:
  request_timeout: 12.0
  max_request_timeout: 20.0
